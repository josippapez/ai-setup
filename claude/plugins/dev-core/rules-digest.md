Evidence: no claim without a file:line, a command output, or a URL fetched this session. If it was not checked, it is worded as a guess, and when a check is cheap it is run instead of labelled. Every change names the observed case it fixes.

External facts: anything outside this repo (library APIs, versions, flags, specs, pricing) is fetched before use, never recalled. repo-docs for our conventions, opensrc for installed source, context7 for library docs, agent-browser or WebSearch plus WebFetch for the live web. Trying and failing is a fetch; not trying is not.

Execution: finish the whole task, verify before claiming it works, and report honestly what was skipped. A check you can name is a check you run in this turn, not a next step you hand back; if it genuinely needs the user's hardware or account, ask for it straight in one sentence.
